# دليل التثبيت والاستخدام السريع

## الخطوات الأساسية

### المرحلة 1: تثبيت الخادم الخلفي (Backend)

1. **انتقل إلى مجلد الخادم**
```bash
cd backend
```

2. **ثبت الحزم**
```bash
npm install
```

3. **أنشئ ملف `.env`** في المجلد:
```bash
PORT=5000
MONGODB_URI=mongodb://localhost:27017/e-court
JWT_SECRET=your_secret_key_here_change_in_production_12345
JWT_EXPIRE=7d
NODE_ENV=development
ENCRYPTION_KEY=your_32_char_encryption_key_here_12345!
FRONTEND_URL=http://localhost:3000
```

4. **تأكد من وجود MongoDB**
   - إذا لم تكن MongoDB مثبتة، يمكنك استخدام:
   ```bash
   # باستخدام Docker
   docker run -d -p 27017:27017 --name mongodb mongo
   ```

5. **شغّل الخادم**
```bash
npm run dev
```

ستظهر الرسالة:
```
E-Court Backend running on port 5000
MongoDB Connected: localhost
```

### المرحلة 2: تثبيت واجهة المستخدم (Frontend)

1. **انتقل إلى مجلد الواجهة**
```bash
cd frontend
```

2. **ثبت الحزم**
```bash
npm install
```

3. **شغّل التطبيق**
```bash
npm run dev
```

التطبيق سيكون متاح على: `http://localhost:3000`

---

## تسجيل الدخول الأول

### 1. إنشاء حساب جديد

زر `http://localhost:3000/register` واختر:
- **الاسم**: أي اسم
- **البريد الإلكتروني**: `test@example.com`
- **كلمة المرور**: `password123`
- **نوع الحساب**: اختر من بين:
  - مضرور/مواطن (Victim)
  - محام (Lawyer)
  - قاضي (Judge)
  - مسؤول النظام (Admin)

### 2. الدخول إلى لوحة التحكم
بعد التسجيل، ستنتقل تلقائياً إلى لوحة التحكم الخاصة برولك.

---

## الأدوار والصلاحيات

### دور المضرور/المواطن (Victim)
**الصلاحيات:**
- عرض لوحة التحكم الخاصة
- تقديم قضية جديدة
- عرض قضاياه
- عرض تفاصيل القضية
- رفع مستندات آمنة
- إرسال رسائل مشفرة

**الرابط المباشر:** `http://localhost:3000/dashboard`

---

### دور المحام (Lawyer)
**الصلاحيات:**
- عرض القضايا المسندة
- الوصول إلى وثائق القضية
- إرسال رسائل مشفرة
- كتابة ملاحظات حول القضايا

**الرابط المباشر:** `http://localhost:3000/dashboard`

---

### دور القاضي (Judge)
**الصلاحيات:**
- عرض القضايا المسندة
- تحديث حالة القضايا
- كتابة ملاحظات خاصة (غير مرئية للآخرين)
- مراجعة جميع الوثائق
- الموافقة على التحديثات

**الرابط المباشر:** `http://localhost:3000/dashboard/assigned-cases`

---

### دور مسؤول النظام (Admin)
**الصلاحيات:**
- عرض جميع المستخدمين
- تغيير أدوار المستخدمين
- عرض جميع القضايا
- إسناد القضايا للقضاة
- عرض سجل التدقيق الكامل

**الروابط المتاحة:**
- لوحة التحكم: `http://localhost:3000/dashboard`
- إدارة المستخدمين: `http://localhost:3000/dashboard/users`
- جميع القضايا: `http://localhost:3000/dashboard/all-cases`
- سجل التدقيق: `http://localhost:3000/dashboard/audit-logs`

---

## الميزات الرئيسية

### 1. تقديم قضية جديدة (للمضرورين)
```
الخطوات:
1. اذهب إلى: /dashboard/submit-case
2. ملأ البيانات:
   - عنوان القضية
   - وصف تفصيلي
   - مستوى الخطورة
3. يمكنك اختيار: "الحفاظ على الهوية سرية"
4. اضغط "رفع القضية"
```

### 2. رفع مستندات
```
الخطوات:
1. من صفحة تفاصيل القضية
2. اضغط على "رفع مستند"
3. اختر الملف (PDF، صور، Word)
4. الملف سيُشفر تلقائياً قبل التخزين
```

### 3. الرسائل المشفرة
```
الخطوات:
1. ستكون متاحة من صفحة تفاصيل القضية
2. اختر المستقبل (محامي، قاضي، وغيره)
3. الرسالة تُشفر تلقائياً
4. الطرف الآخر يرى الرسالة فقط إذا كان مصرحاً
```

### 4. تحديث حالة القضية (القضاة فقط)
```
الخطوات:
1. من صفحة: /dashboard/assigned-cases
2. اختر القضية
3. غيّر الحالة:
   - مقدمة
   - قيد المراجعة
   - جلسة مجدولة
   - مغلقة
4. أضف ملاحظاتك الخاصة
```

### 5. إدارة المستخدمين (الإداريون فقط)
```
الخطوات:
1. اذهب إلى: /dashboard/users
2. اختر مستخدماً
3. غيّر دوره
4. احفظ التغييرات
```

---

## أمثلة استخدام API

### تسجيل جديد
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "fullName": "محمد أحمد",
    "email": "test@example.com",
    "password": "password123",
    "phone": "+966 55 1234 5678",
    "role": "victim"
  }'
```

### تسجيل الدخول
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

### تقديم قضية جديدة
```bash
curl -X POST http://localhost:5000/api/cases/submit \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "title": "قضية تحرش إلكتروني",
    "description": "وصف تفصيلي للقضية...",
    "severity": "high",
    "isAnonymous": true
  }'
```

### الحصول على القضايا
```bash
curl -X GET http://localhost:5000/api/cases/my-cases \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## استكشاف الأخطاء

### الخادم لا يعمل
```
تحقق من:
1. هل MongoDB قيد التشغيل؟
2. هل المنفذ 5000 متاح؟
3. هل ملف .env موجود ومملوء بشكل صحيح؟
4. جرب: lsof -i :5000 (على Mac/Linux)
```

### لا يمكن الوصول للموقع
```
تحقق من:
1. هل التطبيق قيد التشغيل؟
2. هل المنفذ 3000 متاح؟
3. افتح: http://localhost:3000
4. تحقق من وحدة تحكم المتصفح (F12) للأخطاء
```

### خطأ في المصادقة
```
تأكد:
1. التوكن منتسخ بشكل صحيح
2. التوكن لم ينتهي صلاحيته
3. إعادة تسجيل الدخول جديداً
4. تنظيف ذاكرة التخزين المؤقت
```

---

## نصائح الأمان

1. **غيّر المفاتيح السرية** قبل النشر على الإنتاج
2. **استخدم HTTPS** في بيئة الإنتاج
3. **قم بنسخ احتياطية** لقاعدة البيانات بانتظام
4. **راقب سجل التدقيق** بحثاً عن أنشطة مريبة
5. **غيّر كلمات المرور** المضمنة الافتراضية
6. **استخدم متغيرات البيئة** لجميع المعلومات الحساسة

---

## الخطوات التالية

بعد التثبيت الناجح، يمكنك:

1. **اختبار الميزات**: جرب جميع الأدوار
2. **التخصيص**: عدّل الألوان والرسائل حسب احتياجاتك
3. **إضافة مستخدمين حقيقيين**: تتبع العمليات الفعلية
4. **إعداد النسخ الاحتياطية**: استعد للإنتاج
5. **النشر**: استضف على خادم حقيقي

---

## الدعم والمساعدة

للمزيد من المعلومات، راجع:
- `README.md` - نظرة عامة على المشروع
- `API_DOCUMENTATION.md` - وثائق API الكاملة
- مجلد `backend/src` - كود الخادم
- مجلد `frontend/src` - كود الواجهة

---

نتمنى لك تجربة سلسة! 🚀
