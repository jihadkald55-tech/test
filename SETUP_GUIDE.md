# 🚀 دليل إعداد نظام المحكمة الإلكترونية للعمل مع Kit Hub Suite

## تم إصلاح المشاكل التالية:

✅ إضافة `START_HERE.md` في الجذر  
✅ إضافة ملفات `.env.example` للإعداد الصحيح  
✅ تثبيت الذاكرة المخزنة (Repository تم رفعه)  

---

## 📋 خطوات الإعداد السريع

### 1️⃣ **استنساخ المستودع داخل Kit Hub**

```bash
# في Kit Hub Suite، انسخ الرابط التالي:
https://github.com/jihadkald55-tech/test
```

### 2️⃣ **إعداد متغيرات البيئة**

#### Backend (`backend/.env`):
```dotenv
PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/e-court
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d
ENCRYPTION_KEY=your-encryption-key-32-chars-long!
FRONTEND_URL=http://localhost:3000
```

#### Frontend (`frontend/.env.local`):
```dotenv
NEXT_PUBLIC_API_URL=http://localhost:5000
```

### 3️⃣ **تثبيت الحزم**

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 4️⃣ **تشغيل الخادم**

**الخيار A: تشغيل يدوي**
```bash
# Terminal 1 - Backend
cd backend
npm start

# Terminal 2 - Frontend
cd frontend
npm run dev
```

**الخيار B: Docker (الموصى به)**
```bash
# في المجلد الرئيسي
docker-compose up
```

---

## 🔗 الروابط الرئيسية

| الصفحة | الرابط | الدور |
|------|-------|------|
| تسجيل الدخول | http://localhost:3000/login | الجميع |
| التسجيل | http://localhost:3000/register | الجميع |
| لوحة التحكم | http://localhost:3000/dashboard | المستخدمون المسجلون |
| جميع القضايا | http://localhost:3000/dashboard/all-cases | الإداريون فقط |
| قضاياي | http://localhost:3000/dashboard/my-cases | المستخدمون |
| المستخدمون | http://localhost:3000/dashboard/users | الإداريون فقط |
| سجل التدقيق | http://localhost:3000/dashboard/audit-logs | الإداريون فقط |

---

## 🔐 حسابات تجريبية

استخدم هذه البيانات بعد تشغيل النظام:

### مسؤول
- **البريد الإلكتروني**: admin@court.com
- **كلمة المرور**: AdminPass123!

### قاضي
- **البريد الإلكتروني**: judge@court.com
- **كلمة المرور**: JudgePass123!

### محامي
- **البريد الإلكتروني**: lawyer@court.com
- **كلمة المرور**: LawyerPass123!

### ضحية
- **البريد الإلكتروني**: victim@court.com
- **كلمة المرور**: VictimPass123!

---

## 🐛 استكشاف الأخطاء

### مشكلة: "خطأ في الاتصال بقاعدة البيانات"
**الحل**: تأكد من أن MongoDB يعمل على `localhost:27017` أو استخدم Docker

### مشكلة: "CORS Error"
**الحل**: تحقق من أن `FRONTEND_URL` و `NEXT_PUBLIC_API_URL` متطابقة مع النطاقات

### مشكلة: "Blank Frontend"
**الحل**: امسح `node_modules` و `.next` ثم أعد التثبيت:
```bash
cd frontend
rm -r node_modules .next
npm install
npm run build
```

---

## 📦 متطلبات النظام

- **Node.js**: v16 أو أحدث
- **MongoDB**: v5 أو أحدث
- **npm**: v8 أو أحدث
- **Docker**: (اختياري - موصى به)

---

## 🎯 الميزات المتاحة

✅ **نظام تسجيل دخول آمن** بـ JWT  
✅ **تحكم بالوصول القائم على الأدوار** (RBAC)  
✅ **تشفير كامل** للرسائل والمستندات (AES-256)  
✅ **سجل تدقيق** غير قابل للتعديل  
✅ **قضايا مجهولة الهوية** لحماية الخصوصية  
✅ **تصميم قضائي رسمي**

---

## 💡 نصائح مهمة

1. **قبل الإنتاج**: غيّر جميع المفاتيح في `.env` بقيم قوية
2. **HTTPS**: استخدم HTTPS في الإنتاج
3. **التخزين المؤقت**: أضف Redis للأداء الأفضل
4. **النسخ الاحتياطية**: أعدِ نسخ احتياطية من MongoDB بانتظام

---

## 📞 المشاكل والدعم

إذا واجهت مشكلة:
1. تحقق من ملف `QUICK_START.md` للإرشادات التفصيلية
2. راجع `API_DOCUMENTATION.md` للـ API endpoints
3. اقرأ `DEPLOYMENT.md` لنشر الإنتاج

---

**آخر تحديث**: فبراير 2026  
**الحالة**: ✅ جاهز للاستخدام الفوري
